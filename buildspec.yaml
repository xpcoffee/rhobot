version: 0.2

phases:
    pre_build:
        commands:
            - echo "[STATUS] Signing in to ECR"
            - $(aws ecr get-login --no-include-email --region $AWS_DEFAULT_REGION)
    build:
        commands:
            - echo "[STATUS] Rendering template files"
            - npm run renderTemplate
            - export DOCKER_IMAGE_TAG="rhobot:$(git rev-parse HEAD)"
            - echo "[STATUS] Building Docker image for ${IMAGE_TAG}"
            - docker build -t "${IMAGE_TAG}" .
            - docker tag "${IMAGE_TAG}" "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/${IMAGE_TAG}"
    post_build:
        commands:
            - echo "[STATUS] Build complete"
            - echo "[STATUS] Pushing Docker image to ECR"
            - docker push "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/${IMAGE_TAG}"
artifacts:
    files:
        - pull-docker-image
        - application-start
        - appspec.yml
        - creds.yaml
